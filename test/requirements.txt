# Testing Requirements
# Install backend requirements first: pip install -r ../backend/requirements.txt
# Then install these test-specific packages: pip install -r requirements.txt

pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.24.0
pytest-mock==3.14.0
faker==33.1.0
